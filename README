These are my dotfiles, originally cribbed from the examples of more experienced folks.

### USAGE:
1. Clone the repository
2. (Optional) Create a new branch for the host you just cloned on
3. Run `./setup.sh`
